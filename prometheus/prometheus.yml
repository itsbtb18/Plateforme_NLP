global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "django-scraping"
    metrics_path: /scraping/metrics/
    scrape_interval: 15s
    scrape_timeout: 10s
    static_configs:
      - targets: ["django:8000"]
